#!/usr/bin/env bash
set -e

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
BACKEND_ROOT="$(CDPATH= cd -- "$SCRIPT_DIR/../../.." && pwd)"

export PYTHONPATH="$BACKEND_ROOT${PYTHONPATH:+:$PYTHONPATH}"
exec "$BACKEND_ROOT/.venv/bin/python" "$@"
